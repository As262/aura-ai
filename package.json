{
  "name": "aura-ai",
  "version": "1.0.0",
  "description": "Aura AI - Aesthetic Analysis and Conversation Decoding Platform",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && python manage.py runserver",
    "frontend": "cd frontend && npm start",
    "setup": "npm run setup-backend && npm run setup-frontend",
    "setup-backend": "cd backend && python -m venv venv && venv\\Scripts\\activate && pip install -r requirements.txt && python manage.py migrate",
    "setup-frontend": "cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "test": "npm run test-frontend && npm run test-backend",
    "test-frontend": "cd frontend && npm test",
    "test-backend": "cd backend && python manage.py test"
  },
  "keywords": ["ai", "aesthetic", "analysis", "conversation", "react", "django"],
  "author": "Aura AI Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "engines": {
    "node": ">=16.0.0",
    "python": ">=3.8.0"
  }
}